import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Nouakchott-immobillier',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
