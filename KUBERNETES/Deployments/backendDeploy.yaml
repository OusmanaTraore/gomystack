apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: web-mysql
    tier: backend
  name: mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-mysql
      tier: backend
  strategy: {}
  template:
    metadata:
      labels:
        app: web-mysql
        tier: backend
    spec:
      containers:
      - image: hshar/mysql:5.5
        name: mysql
        ports:
        - containerPort: 3306
        resources: {}
status: {}
